# Debian 12 notes

Этот проект больше не использует Docker. Актуальная инструкция находится в:

```txt
docs/BAREMETAL_DEBIAN12.md
```

Короткий путь:

```bash
sudo bash scripts/install-debian12-baremetal.sh
sudo PROJECT_DIR=/opt/newdomofon-video bash scripts/deploy-debian12-baremetal.sh
```
